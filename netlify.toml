[functions]
  node_bundler = "esbuild"
  directory = "functions"
  external_node_modules = ["tedious", "mysql", "mysql2", "oracledb", "pg", "pg-query-stream"]
  included_files = ["data/*"]
[build]
  publish="dist"
[dev]
  command = "npm run dev"
  targetPort=5173
  port=8888